<?xml version="1.0" encoding="UTF-8"?>
<!--
  역할: 카드 2장 지급/리롤/선택 완료 (20초 제한)
  핵심기능: 초기 2장 지급 / 리롤(40pt) / 시간 만료 자동 확정 / 선택 결과 전송
-->

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.geometry.*?>

<AnchorPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.omokpang.controller.cards.CardSelectController"
            prefWidth="750" prefHeight="600">

    <!-- 배경 -->
    <ImageView fitWidth="750" fitHeight="600"
               AnchorPane.leftAnchor="0" AnchorPane.topAnchor="0">
        <image><Image url="/images/bg_darkmode.png" /></image>
    </ImageView>

    <!-- 상단 타이머 -->
    <Label fx:id="timerLabel"
           text="20초"
           style="-fx-font-size: 30px;"
           AnchorPane.topAnchor="20"
           layoutX="370"/> <!-- 전체 800px 기준 중앙쯤 -->

    <!-- 🌟 1단계: 카드 받기 화면 -->
    <VBox fx:id="beforeBox" spacing="22" alignment="TOP_CENTER"
          AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0" AnchorPane.topAnchor="100">

        <Label text="랜덤으로 카드 2장을 부여받습니다."
               style="-fx-text-fill:white; -fx-font-size:26; -fx-font-weight:800;" />

        <Label text="40포인트를 사용해서 랜덤으로 다른 카드로 변경가능"
               style="-fx-text-fill:#cccccc; -fx-font-size:15;" />

        <Label text="정해진 20초가 지나면 카드 완료 버튼을 선택 안해도 게임으로 바로 넘어갑니다."
               style="-fx-text-fill:#cccccc; -fx-font-size:14;" />

        <HBox spacing="60" alignment="CENTER">
            <ImageView fitWidth="140" preserveRatio="true">
                <image><Image url="/images/cards/unknown_card.png" /></image>
            </ImageView>
            <ImageView fitWidth="140" preserveRatio="true">
                <image><Image url="/images/cards/unknown_card.png" /></image>
            </ImageView>
        </HBox>

        <Button fx:id="receiveBtn"
                text="카드 받기"
                onAction="#handleReceive"
                style="-fx-background-color: linear-gradient(#d9d9a0, #8c8c52);
                       -fx-text-fill:black; -fx-font-size:22; -fx-font-weight:800;
                       -fx-background-radius:12; -fx-padding:10 26;"/>
    </VBox>

    <!-- 🌟 2단계: 카드 확인 화면 -->
    <VBox fx:id="afterBox" visible="false"
          spacing="26" alignment="TOP_CENTER"
          AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0" AnchorPane.topAnchor="100">

        <Label text="랜덤으로 카드 2장을 부여받습니다."
               style="-fx-text-fill:white; -fx-font-size:26; -fx-font-weight:800;" />

        <Label text="40포인트를 사용해서 랜덤으로 다른 카드로 변경가능"
               style="-fx-text-fill:#cccccc; -fx-font-size:15;" />

        <Label text="정해진 20초가 지나면 카드 완료 버튼을 선택 안해도 게임으로 바로 넘어갑니다."
               style="-fx-text-fill:#cccccc; -fx-font-size:14;" />

        <HBox spacing="60" alignment="CENTER">

            <!-- 카드 1 -->
            <VBox spacing="10" alignment="CENTER">
                <ImageView fx:id="cardImage1" fitWidth="140" preserveRatio="true" />
                <Button text="↻"
                        fx:id="rerollBtn1"
                        onAction="#handleReroll1"
                        style="-fx-background-color: rgba(255,255,255,0.2);
                               -fx-text-fill:white; -fx-font-size:14; -fx-font-weight:700;
                               -fx-background-radius:20; -fx-padding:6 16;" />
                <Label text="★ 40 차감"
                       style="-fx-text-fill:white; -fx-font-size:14; -fx-font-weight:700;" />
            </VBox>

            <!-- 카드 2 -->
            <VBox spacing="10" alignment="CENTER">
                <ImageView fx:id="cardImage2" fitWidth="140" preserveRatio="true" />
                <Button text="↻"
                        fx:id="rerollBtn2"
                        onAction="#handleReroll2"
                        style="-fx-background-color: rgba(255,255,255,0.2);
                               -fx-text-fill:white; -fx-font-size:14; -fx-font-weight:700;
                               -fx-background-radius:20; -fx-padding:6 16;" />
                <Label text="★ 40 차감"
                       style="-fx-text-fill:white; -fx-font-size:14; -fx-font-weight:700;" />
            </VBox>

        </HBox>

        <Button fx:id="completeBtn"
                text="카드 선택 완료"
                onAction="#handleComplete"
                style="-fx-background-color: linear-gradient(#d9d9a0, #8c8c52);
                       -fx-text-fill:black; -fx-font-size:22; -fx-font-weight:800;
                       -fx-background-radius:12; -fx-padding:10 26;"/>
    </VBox>

    <!-- 좌측 하단 카드 종류 버튼 -->
    <HBox spacing="6"
          alignment="CENTER_LEFT"
          AnchorPane.leftAnchor="20"
          AnchorPane.bottomAnchor="20">

        <!-- 별 텍스트 -->
        <Label text="★"
               style="-fx-text-fill: #ffd863; -fx-font-size: 18; -fx-font-weight: 900;" />


        <!-- 보유 포인트 -->
        <Label fx:id="pointLabel"
               text="0"
               style="-fx-text-fill:white; -fx-font-size:16; -fx-font-weight:700;"/>

        <!-- 카드 종류 버튼 -->
        <Button onAction="#handleOpenCatalog"
                style="-fx-background-color: transparent; -fx-padding: 0;">
            <graphic>
                <Label text="카드 종류"
                       style="-fx-text-fill:white; -fx-font-size:16; -fx-font-weight:700;"/>
            </graphic>
        </Button>

    </HBox>


</AnchorPane>